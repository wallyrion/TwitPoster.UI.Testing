import { environment } from '../../../environments/environment';

export const apiBaseUrl = environment.apiUrl;
export const googleClientId: string = environment.googleClientId;
export const clientAppUrl: string = environment.clientAppUrl;
